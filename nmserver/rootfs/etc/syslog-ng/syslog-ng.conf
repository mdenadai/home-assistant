@version: 4.3
@include "scl.conf"

log {
   source { network(transport(udp)); };
   source { network(transport(tcp)); };
   destination {
      file(
         "/config/nmserver/syslog/${HOST}/${YEAR}_${MONTH}.log"
         create-dirs(yes)
      );
   };
};
